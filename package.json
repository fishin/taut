{
  "name": "taut",
  "version": "1.1.0",
  "description": "queue for ficion",
  "main": "lib/index.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/fishin/taut"
  },
  "engines": {
    "node": ">=4"
  },
  "scripts": {
    "clean": "rm -rf node_modules",
    "console": "lab -a code -t 100 -v -m 5000 -L",
    "test": "lab -a code -t 100 -v -m 5000 -L -r json -o test.lab -r html -o test.html -r console -o stdout -r console -o test.log"
  },
  "author": "Lloyd Benson",
  "license": "MIT",
  "dependencies": {
    "hoek": "2.x.x"
  },
  "devDependencies": {
    "code": "1.x.x",
    "lab": "7.x.x"
  }
}
